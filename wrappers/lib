#!/usr/bin/env bash

. "$(dirname "$0")"/msvcenv.sh

rsp=$(mktemp "${TMPDIR:-/tmp}/lib.XXXXXX.rsp") || exit 1
trap 'rm -f "$rsp"' EXIT

for arg in "$@"; do
    printf '%s\n' "$arg" >>"$rsp"
done

"$(dirname "$0")"/wine-msvc.sh "$BINDIR"/lib.exe "@$rsp"
